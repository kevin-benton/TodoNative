/**
 * @format
 */

import { AppRegistry } from 'react-native';

import App from 'TodoNative/App/Containers/App';
import { name as appName } from 'TodoNative/app.json';

AppRegistry.registerComponent(appName, () => App);
